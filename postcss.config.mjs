/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
    '@tailwindcss/postcss': {}, // Tailwind v4-er jonno eta proyojon
    'autoprefixer': {},
  },
};

export default config;